# language: zh-CN
@session @mvp
功能: 聚合并精确打开 Agent 内部会话
  为了集中发现需要介入的 Agent 工作
  作为同时使用多个 AI Agent 的本地用户
  我希望查看统一的内部会话状态并准确回到原会话

  场景: 同一 Agent 的多个内部会话分别展示
    假如一个 Agent 中存在三个具有不同原生会话标识的内部会话
    当 AgentHub 完成会话发现
    那么会话列表应显示三个独立会话
    并且每个会话应保留自己的状态、项目和原生会话标识

  场景: 首页按内部会话而不是进程统计
    假如一个 Agent 进程中同时存在三个内部会话
    当 AgentHub 生成首页统计
    那么会话总数应增加三个
    并且不得用 Agent 进程数或窗口数替代会话数

  场景: Agent 未提供规划时不推测进度
    假如一个内部会话具有状态和最近活动但 Agent 未提供规划
    当 AgentHub 展示该会话
    那么会话卡片应正常展示已获得的信息
    并且不得生成规划步骤或虚假完成百分比

  场景: Agent 提供规划时按原始信息展示
    假如一个内部会话提供了当前目标、计划步骤和 Todo
    当 AgentHub 展示该会话
    那么会话卡片应展示 Agent 提供的规划信息
    并且不得改写步骤状态或自行补充计划

  场景: 精确打开指定内部会话
    假如列表中存在一个具有稳定原生会话标识的可恢复会话
    当用户对该会话执行“打开会话”
    那么 AgentHub 应调用对应 Agent 的恢复能力
    并且恢复目标应指向该会话而不是仅打开 Agent 首页

  场景: 会话需要用户输入时回到原生 Agent
    假如一个内部会话正在等待用户输入或权限批准
    当用户在 AgentHub 中查看该会话
    那么 AgentHub 应展示待处理原因和打开会话入口
    并且不应提供直接回复或批准权限的操作

  场景: 只能激活工具窗口时标记为有限接入
    假如一个 Agent 只能被检测为正在运行且不能识别内部会话
    当 AgentHub 展示该 Agent 的接入能力
    那么该 Agent 的会话接入等级应为有限接入
    并且该 Agent 不应计入核心会话统计
