# language: zh-CN
@session @attention @mvp
功能: 自动识别并解除需要用户介入的会话状态
  为了让用户只关注真正需要处理的 Agent 会话
  作为同时使用多个 AI Agent 的本地用户
  我希望待处理状态由可信的原生事件自动维护

  场景大纲: 自动识别需要人工处理的会话
    假如某个内部会话的统一状态为“<状态>”
    当 AgentHub 汇总待处理会话
    那么该会话应出现在待处理列表中

    例子:
      | 状态 |
      | 等待授权 |
      | 等待输入 |
      | 等待验收 |
      | 执行中断 |

  场景大纲: 会话恢复执行后自动解除待处理
    假如某个内部会话因为“<原因>”处于待处理状态
    当对应 Agent 上报该会话已经恢复执行
    那么 AgentHub 应自动解除该待处理原因
    并且不得要求用户手工标记为已处理

    例子:
      | 原因 |
      | 等待授权 |
      | 等待输入 |
      | 执行中断 |

  场景: 仅打开会话不解除等待验收
    假如某个内部会话正在等待用户验收
    当用户通过 AgentHub 打开该会话但未继续发送指令或明确接受结果
    那么该会话仍应保持等待验收

  场景: 原生事件表明用户继续处理后解除等待验收
    假如某个内部会话正在等待用户验收
    当 Agent 上报用户继续发送指令、明确接受结果或关闭会话
    那么 AgentHub 应自动解除等待验收

  场景: 状态来源不可靠时保留不确定性
    假如 AgentHub 只能从日志或窗口行为推测一个会话的状态
    当系统展示该状态和待处理判断
    那么界面应同时展示状态来源和可信度
    并且不得把推测结果表示为 Agent 明确事件

  场景: 用户标记暂停会话供后续跟进
    假如某个会话当前不需要系统介入
    当用户为该会话添加待跟进标记、标签和备注
    那么该会话应出现在用户跟进列表中
    并且会话的原生运行状态和系统关注状态保持不变

  场景: 取消跟进标记不清除真实待处理状态
    假如某个会话既被用户标记为待跟进又正在等待 Agent 授权
    当用户取消待跟进标记
    那么用户跟进状态应被清除
    并且等待授权的系统关注状态仍然保留
