---
status: accepted
date: 2026-07-18
---

# 按职责分离源代码与文档

> 文档目录的具体分类已由 ADR-0006 更新；本决策中 `src/` 与 `docs/` 的职责分离仍然有效。

## 决策

运行代码和参考实现统一放在 `src/`；文档统一放在 `docs/`，并划分为需求、架构决策、测试、原型、指南、规划和经验记录。

仓库根目录仅保留项目导航、依赖、许可证和启动入口。

## 影响

导入路径和启动脚本需要显式使用 `src`。历史需求和原型可以继续保留，但不会再被误认为运行代码。
