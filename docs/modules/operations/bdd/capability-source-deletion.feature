# language: zh-CN
@capability @deletion @destructive
功能: 区分能力安装卸载与能力来源删除
  为了避免把取消单个 Agent 安装误操作成全局内容删除
  作为管理共享能力的本地用户
  我希望系统在删除来源前展示完整影响并要求明确确认

  场景: 卸载普通共享安装时保留来源
    假如一个能力来源被三个 Agent 的共享安装引用
    当用户从其中一个非来源 Agent 卸载该能力安装
    那么系统应只移除该 Agent 的链接和原生注册
    并且能力来源和另外两个 Agent 的安装应保持可用

  场景大纲: 从来源位置卸载时识别为全局删除
    假如“<能力类型>”的来源被多个 Agent 安装引用
    当用户从来源所在位置发起卸载
    那么系统应将操作标记为“删除能力来源”
    并且应展示所有受影响的 Agent 安装和插件引用
    并且在用户明确确认前不得修改任何内容

    例子:
      | 能力类型 |
      | Skill |
      | MCP Server |
      | Agent 插件 |

  场景: 用户取消来源删除确认
    假如系统已经展示来源删除的完整影响范围
    当用户取消确认
    那么能力来源、共享链接、原生配置和安装状态都应保持不变

  场景: 来源删除预检失败
    假如某个依赖安装无法安全取消原生注册
    当系统执行来源删除预检
    那么系统不得删除能力来源
    并且应报告阻止删除的安装和原因

  场景: 用户确认删除能力来源
    假如来源删除预检已经通过
    并且用户已经确认全局影响
    当系统执行来源删除
    那么所有依赖安装应被安全移除或标记为缺失
    并且活动能力来源应不再存在
    并且隔离备份不得显示为已安装能力
    并且完整操作应写入审计记录

  场景: 永久清除来源恢复备份
    假如已经删除的能力来源仍有可恢复备份
    当用户发起永久清除但尚未再次确认
    那么系统不得删除恢复备份
    当用户明确确认永久清除
    那么系统可以删除恢复备份
    并且应提示该内容无法再通过 AgentHub 恢复
