# 仓库脚本

本目录用于保存可重复执行的维护、迁移、测试夹具准备和验证脚本。为方便一键运行，应用启动脚本继续保留在仓库根目录。

会修改用户能力目录的脚本必须校验绝对目标路径；条件允许时应支持 `dry-run`；同时必须说明备份和恢复方式。

源码索引维护直接使用 CodeGraph CLI，不在本目录重复封装索引数据库或生成脚本。初始化、同步和查询命令见 [CodeGraph 使用指南](../docs/guides/codegraph.md)。
